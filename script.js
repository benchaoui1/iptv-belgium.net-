"use strict";document.documentElement.classList.add("js");const toggle=document.querySelector(".nav-toggle");const nav=document.querySelector(".main-nav");if(toggle&&nav){toggle.addEventListener("click",()=>{const open=nav.classList.toggle("open");toggle.setAttribute("aria-expanded",open);});document.addEventListener("click",e=>{if(!toggle.contains(e.target)&&!nav.contains(e.target)){nav.classList.remove("open");toggle.setAttribute("aria-expanded","false");}});document.addEventListener("keydown",e=>{if(e.key==="Escape"){nav.classList.remove("open");toggle.setAttribute("aria-expanded","false");toggle.focus();}});}
const reduced=window.matchMedia("(prefers-reduced-motion:reduce)").matches;if(!reduced&&"IntersectionObserver"in window){const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("in");io.unobserve(e.target);}});},{rootMargin:"0px 0px -5% 0px",threshold:0.07});document.querySelectorAll(".reveal,.reveal-left,.reveal-scale").forEach(el=>io.observe(el));}else{document.querySelectorAll(".reveal,.reveal-left,.reveal-scale").forEach(el=>el.classList.add("in"));}
if(!reduced&&window.matchMedia("(pointer:fine)").matches){let raf=false;window.addEventListener("pointermove",e=>{if(raf)return;raf=true;requestAnimationFrame(()=>{document.documentElement.style.setProperty("--mx",`${e.clientX}px`);document.documentElement.style.setProperty("--my",`${e.clientY}px`);raf=false;});},{passive:true});}
if(!reduced&&window.matchMedia("(pointer:fine)").matches){document.querySelectorAll(".device-frame").forEach(el=>{el.addEventListener("mousemove",e=>{const r=el.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-0.5;const y=(e.clientY-r.top)/r.height-0.5;el.style.transform=`perspective(900px)rotateY(${x*-8}deg)rotateX(${y*5}deg)`;});el.addEventListener("mouseleave",()=>{el.style.transform="";});});}
document.querySelectorAll("[data-copy]").forEach(btn=>{btn.addEventListener("click",async()=>{const val=btn.getAttribute("data-copy");try{await navigator.clipboard.writeText(val);const orig=btn.textContent;btn.textContent="✓ Copié";setTimeout(()=>{btn.textContent=orig;},1500);}catch{btn.textContent=val;}});});document.querySelectorAll(".faq-item summary").forEach(s=>{s.addEventListener("keydown",e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();s.parentElement.toggleAttribute("open");}});});document.querySelectorAll("picture img:not([fetchpriority='high'])").forEach(img=>{if(!img.hasAttribute("loading")){img.setAttribute("loading","lazy");img.setAttribute("decoding","async");}});document.querySelectorAll(".stagger-children").forEach(parent=>{Array.from(parent.children).forEach((child,i)=>{child.style.transitionDelay=`${i*80}ms`;});});